module.exports=class Items{
    
    
    /*
    This function gets the current stocks in the database,
    currently it is returning 50 in all cases
    */
    getCurrentItemStock(){
        //Get stocks from database
        let stocks = 50;
        return stocks;    
    }
}